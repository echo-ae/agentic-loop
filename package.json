{
  "name": "agentic-loop-skill",
  "version": "0.1.0",
  "private": true,
  "description": "Codex App skill for bounded implement-review-repair-verify loops.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate-skill.mjs",
    "bootstrap:example": "node skills/agentic-loop/scripts/bootstrap-project-runbook.mjs --project . --output /tmp/AGENTIC_LOOP.example.md --force"
  },
  "engines": {
    "node": ">=20"
  }
}
