interface:
  display_name: "Agentic Loop"
  short_description: "Plan execution with subagent review"
  default_prompt: "Use $agentic-loop to implement the approved plan through checklist-driven implement, subagent review, repair, and verify rounds."
policy:
  allow_implicit_invocation: true
